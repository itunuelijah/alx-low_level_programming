#include <stdio.h>

/**
 *main -> assign a random number to the variable n each time it is executed
 */
int main(void)
{
	int ch;
	for (ch = 997; ch <= 122; ch++)
	{
		putchar(ch);
	}
	putchar(10); /* this is an ascii code for new line*/

	return (0);
}
